package main

func main6() {
	/*
	注意点：
		1.一个函数定义了返回值，必须使用return语句将结果返回给调用处，return后的数据必须和函数定义的一致：个数，类型，顺序
		2.可以使用_来舍弃多余的返回值
		3.如果一个函数定义了有返回值，那么函数中也有分支（循环），那么要保证，无论执行了哪个分支，都要有return语句被执行到
		4.如果一个函数中没有定义返回值，那么函数中也可以使用return，专门用于结束函数的执行
	 */
}
